[pytest]
markers =
	slow: Slow tests
	nolocal: Not local tests
	odbc: Database tests
	parquetTools: Parquet tools tests
	first: Run test first
	last: Run test last
	mayfail: Might fail

#log_cli = true
#log_format = %(asctime)s %(levelname)s %(message)s
#log_date_format = %Y-%m-%d %H:%M:%S
#log_level = DEBUG
